<div class="burger-menu">
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="container u-padding-leftright-md">
            <div class="burger-menu__logo">
                <a class="logo-text" href="{{ path('homepage') }}">
                    KFU
{#                    <img src="{{ asset('images/logo FFKR.png') }}" alt="">#}
                </a>
            </div>

            <div class="languages languages--mobile">
                {% include('switch_locales/switch_locales.html.twig') %}
            </div>

            <div class="user-avatar">
                <button id="btnMenu" class="burger-menu__button collapsed" data-toggle="collapse"
                        data-target="#navbarTogglerDemo02"
                        aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">Burger
                    button
                </button>
            </div>
        </div>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <div class="container">
                <div class="burger-menu__navbar">
                    <!-- меню первого ряда -->
                    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                        {% for item in mine_menu %}
                            <li class="nav-item" id="burgerAdd">
                                <div class="nav-link">
                                    {{ item.title }}
                                </div>

                                <div id="burgerAddContent" class="burger-menu__add">
                                    <ul class="burger-menu__add-list">
                                        {% for subMenu in item.children %}
                                            <li class="burger-menu__add-item u-margin-bottom-medium">
                                                <a href="{{ path('showPages', {'slug': subMenu.slug}) }}"
                                                   class="burger-menu__add-link">
                                                    {{ subMenu.title }}
                                                </a>
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </nav>
</div>