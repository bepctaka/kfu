{% extends 'base.html.twig' %}

{% block body %}
    <main id="main">
        <div class="container">
            <section class="section-news">
                <!--не обращать внимание на имя класса-->
                <div class="row">
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="theme-title">
                            <h4 class="heading-secondary u-margin-top-medium u-margin-bottom-medium ">
                                {{ page.title }}
                            </h4>
                        </div>
                    </div>
                </div>
                <button class="scroll-to-top"><img src="{{ asset('images/arrows/arrow.btnTop.svg') }}" alt=""></button>
                <div class="full-news">
{#                    {% if page.url is defined %}#}

{#                        <div  class="full-news__img"#}
{#                              #}
{#                              style="background-image: url({{ vich_uploader_asset(page, 'imageFile') }}); display: flex; justify-content: center; align-items: center;"#}
{#                             onclick="$(this).replaceWith('<iframe width=\'100%\' height=\'200\' ' +#}
{#                         'src=\'{{ page.url }}\' frameborder=\'0\' allowfullscreen></iframe><br/>');"#}
{#                              #}
{#                        >#}
{#                            <iframe src="{{ page.url }}" width="100%" height="100%">#}
{#                            </iframe>#}
{#                        </div>#}
{#                    {% else %}#}
                        <div class="full-news__img"
                             style="background-image: url({{ vich_uploader_asset(page, 'imageFile') }})">
                        </div>
{#                    {% endif %}#}
                    <div class="full-news__descr">
                        {#<strong style="font-size: 28px">L</strong>#}
                        {{ page.description | raw}}

                    </div>
                </div>
            </section>
        </div>
    </main>
{% endblock %}